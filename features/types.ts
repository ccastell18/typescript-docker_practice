const today = new Date()
//typing today. will bring up a menu of functions that today can use because today has been defined as a Date, so it'll show date functions
today.getMonth()

const person = {
  age: 20
}

class Color{

}
const red = new Color()
//red would have access to any properties created in the Color class